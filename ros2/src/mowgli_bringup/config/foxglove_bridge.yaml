foxglove_bridge:
  ros__parameters:
    port: 8765
    address: "0.0.0.0"
    send_buffer_limit: 10000000
    num_threads: 0
    # Whitelist params omitted — foxglove_bridge exposes all topics/services
    # by default. Add entries to restrict, e.g.:
    # topic_whitelist: ["/scan", "/cmd_vel"]
